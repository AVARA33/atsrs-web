# Keep paid billing closed until the bank merchant account, official adapter,
# webhook signature verification and staging certification are complete.
ATSRS_BILLING_ENABLED=false
ATSRS_PAYMENT_PROVIDER=

# Provider credentials and webhook secrets belong in Supabase project secrets.
# Never commit real values or expose them to browser JavaScript.
