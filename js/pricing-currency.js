/* Currency display for ATSRS plan prices. Base prices remain USD. */
(function(){
  'use strict';
  var KEY='atsrs_pricing_currency',API='https://cdn.jsdelivr.net/gh/irfanokr/currency-api@main/v1/currencies/usd.min.json';
  var fallback={USD:1,AZN:1.7,EUR:.86,GBP:.75,AED:3.6725,TRY:41.2,SAR:3.75,CAD:1.38,AUD:1.52,CHF:.8,JPY:147.2,CNY:7.12,INR:88.1};
  function saved(){try{return(localStorage.getItem(KEY)||((window.atsrsI18n&&window.atsrsI18n.getLocale&&window.atsrsI18n.getLocale()==='az')?'AZN':'USD')).toUpperCase()}catch(_){return'USD'}}
  function format(value,code){try{return new Intl.NumberFormat(undefined,{style:'currency',currency:code,minimumFractionDigits:2,maximumFractionDigits:2}).format(value)}catch(_){return code+' '+value.toFixed(2)}}
  function base(el,yearly){var raw=yearly?el.dataset.yearly:el.dataset.monthly,match=String(raw||'0').match(/[0-9][0-9,.]*/);return match?(Number(match[0].replace(/,/g,''))||0):0}
  function render(root,rates){var code=saved(),rate=rates[code]||1,yearly=false;var y=root.querySelector('[data-pricing-cycle="yearly"]');if(y)yearly=y.getAttribute('aria-pressed')==='true';root.querySelectorAll('[data-plan-price]').forEach(function(el){var strong=el.querySelector('strong');if(strong)strong.textContent=format(base(el,yearly)*rate,code)});root.querySelectorAll('[data-price-copy]').forEach(function(el){el.textContent=format(base(el,yearly)*rate,code)+(yearly?' / ay':' / ay')});var free=root.querySelector('[data-free-price]');if(free)free.textContent=format(0,code)}
  async function mount(root){if(!root||root.querySelector('[data-currency-picker]'))return;var hero=root.querySelector('.pricing-hero');if(!hero)return;var wrap=document.createElement('label');wrap.className='pricing-currency-picker';wrap.dataset.currencyPicker='1';wrap.innerHTML='<span>Valyuta</span><select aria-label="Valyuta"></select>';hero.prepend(wrap);var select=wrap.querySelector('select'),rates=fallback;try{var cached=JSON.parse(localStorage.getItem('atsrs_fx_cache')||'null');if(cached&&Date.now()-cached.time<86400000)rates=cached.rates;else{var data=await fetch(API,{cache:'force-cache'}).then(function(r){if(!r.ok)throw Error();return r.json()});rates={};Object.keys(data.usd||{}).forEach(function(k){rates[k.toUpperCase()]=data.usd[k]});localStorage.setItem('atsrs_fx_cache',JSON.stringify({time:Date.now(),rates:rates}))}}catch(_){rates=fallback}Object.keys(rates).sort().forEach(function(code){var o=document.createElement('option');o.value=code;o.textContent=code;select.appendChild(o)});if(!rates[saved()])try{localStorage.setItem(KEY,'USD')}catch(_){}select.value=saved();select.onchange=function(){try{localStorage.setItem(KEY,select.value)}catch(_){}render(root,rates)};root.querySelectorAll('[data-pricing-cycle]').forEach(function(b){b.addEventListener('click',function(){queueMicrotask(function(){render(root,rates)})})});window.addEventListener('atsrs:locale-changed',function(){render(root,rates)});render(root,rates)}
  window.atsrsPricingCurrency={mount:mount};
})();
